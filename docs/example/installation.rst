============
Installation
============

At the command line::

    $ easy_install django-getpaid

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-getpaid
    $ pip install django-getpaid
